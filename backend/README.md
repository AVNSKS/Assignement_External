# Backend

This folder contains the product recommendation API.

## Setup

```bash
npm install
npm run dev
```

Add your secret to `.env`:

```bash
GEMINI_API_KEY=your_api_key_here
PORT=3001
```

The frontend proxies `/api` requests here during local development.
